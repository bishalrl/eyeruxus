import 'package:flutter/material.dart';

class ReviewPartyScreen extends StatelessWidget {
  const ReviewPartyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Review Party')),
      body: const Center(child: Text('Review Party Screen')),
    );
  }
}
