import 'package:flutter/material.dart';

class LiveRoomScreen1 extends StatelessWidget {
  const LiveRoomScreen1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Live Screen')),
      body: const Center(child: Text('Live Screen Page')),
    );
  }
}
